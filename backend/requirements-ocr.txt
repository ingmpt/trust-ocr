# Motor OCR real. Antes usaba PaddlePaddle nativo; se migro a ONNX Runtime
# (rapidocr-onnxruntime, mismos modelos PP-OCRv3) porque PaddlePaddle escalaba su uso de
# RAM proporcional al mem_limit del contenedor sin importar el tamano del documento
# (OOM reproducible de 1024m hasta 2048m). ONNX Runtime no tiene ese comportamiento.
# pip install -r requirements-ocr.txt
setuptools
rapidocr-onnxruntime==1.3.24
