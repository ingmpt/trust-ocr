# Motor OCR real (PaddleOCR, decisión final del sponsor). Dependencia pesada
# (modelos de ML); instalar sólo cuando se vaya a usar OCR_ENGINE=paddleocr.
# pip install -r requirements-ocr.txt
setuptools
paddleocr==2.8.1
paddlepaddle==2.6.2
# pyclipper 1.4.0 trae wheels manylinux rotos (zlib.error al importar _pyclipper);
# se fija a la última versión estable conocida.
pyclipper==1.3.0.post6
