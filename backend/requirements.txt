# --- Framework / API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9

# --- Base de datos ---
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9

# --- Cache / sesión / cola ---
redis==5.0.8
celery==5.4.0

# --- Autenticación / seguridad ---
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# --- Preprocesamiento de imagen ---
opencv-python-headless==4.10.0.84
numpy==1.26.4

# --- LLM de corrección/estructuración (Gemini) ---
google-generativeai==0.8.2

# --- Almacenamiento de objetos (Cloudflare R2, S3-compatible) ---
boto3==1.35.24

# --- Correo transaccional (Zoho SMTP) ---
# se usa smtplib de la librería estándar; sin dependencia adicional.

# --- Utilidades ---
python-dotenv==1.0.1
tenacity==9.0.0
email-validator==2.2.0

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# =============================================================================
# Dependencias pesadas/opcionales — NO incluidas por defecto para evitar
# fallos de instalación en el entorno de desarrollo local:
#   - Motor OCR real: ver requirements-ocr.txt (paddleocr, paddlepaddle).
#     Mientras no se instalen, OCR_ENGINE=mock usa un adaptador sin ML (ver
#     app/services/ocr/engine.py).
#   - Generación de PDF real: ver requirements-pdf.txt (weasyprint, que
#     requiere librerías nativas de GTK en Windows). Mientras no se instale,
#     el Portal ARCO usa un generador de PDF de reemplazo (ver
#     app/services/pdf_generator.py).
# =============================================================================

